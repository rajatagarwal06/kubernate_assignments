1. Check target ports using command kubectl get svc. it shows internal and external port.
2. Check if port is available with netstat command. 
3. If target port of nodeport is not accessable then change to other port and connect via external port 
